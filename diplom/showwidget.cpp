#include "showwidget.h"

#include <QBoxLayout>

using namespace GUI;

ShowWidget::ShowWidget(QWidget *parent) :
    QWidget(parent)
{

//    m_doubleFrameShowWidget = new DoubleFrameShowWidget(insideWidget);
//    m_nameLabel = new QLabel("filename", this);
//    insideShowLayout->addWidget(m_doubleFrameShowWidget);

//    insideShowLayout->addWidget(m_nameLabel);


}
